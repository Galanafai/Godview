# GodView Demo - Requirements
# Ubuntu 22.04 + Python 3.10+

# Core rendering
open3d>=0.18.0
opencv-python>=4.8.0
numpy>=1.24.0

# Video encoding (uses subprocess to call ffmpeg)
# ffmpeg must be installed: sudo apt install ffmpeg

# Utilities
Pillow>=10.0.0
